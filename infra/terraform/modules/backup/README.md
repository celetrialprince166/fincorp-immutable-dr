# module: backup

**Intent (Phase per AGENTS.md roadmap):** AWS Backup vaults in us-east-1 and us-west-2, a backup plan with a daily rule and a cross-region copy action to us-west-2, the AWS Backup IAM service role, and selection of the RDS instance. Uses the aws.use1 and aws.usw2 provider aliases.

> Stub created during scaffolding. Implement in the build conversation following the
> `securing-supply-chain` / `disaster-recovery` skills.
