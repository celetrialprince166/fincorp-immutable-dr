# module: codeartifact

**Intent (Phase per AGENTS.md roadmap):** CodeArtifact domain + npm repo + pip (python) repo, each wired to the public upstream (npm-store / pypi-store). Outputs repo endpoints + domain for CodeBuild auth.

> Stub created during scaffolding. Implement in the build conversation following the
> `securing-supply-chain` / `disaster-recovery` skills.
