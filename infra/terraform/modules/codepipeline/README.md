# module: codepipeline

**Intent (Phase per AGENTS.md roadmap):** CodePipeline: Source (GitHub via a CodeStar connection) -> Build (the codebuild module). Artifact bucket + pipeline IAM role.

> Stub created during scaffolding. Implement in the build conversation following the
> `securing-supply-chain` / `disaster-recovery` skills.
